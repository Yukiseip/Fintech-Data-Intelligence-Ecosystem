apiVersion: 1

datasources:
  - name: PostgreSQL-Fintech-V2
    type: postgres
    uid: postgres-fintech
    url: postgres:5432
    database: fintech_dw
    user: fintech_user
    jsonData:
      database: fintech_dw
      sslmode: "disable"
      maxOpenConns: 10
      maxIdleConns: 5
      postgresVersion: 1400
      timescaledb: false
    secureJsonData:
      password: fintech_secret_2026
    isDefault: true
    editable: false

  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090
    isDefault: false
    editable: false
